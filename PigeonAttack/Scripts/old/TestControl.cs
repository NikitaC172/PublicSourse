using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class TestControl : MonoBehaviour
{/*
    public List<MoveState> moveStates;
    public JoystickPlayerExample player;

    public float playerSpeed;
    public float pigeonSpeed;
    public float pigeonRotate;

    public Text textPlayerSpeed;
    public Text textPigeonSpeed;
    public Text textPigeonRotate;

    public void ChangePlayerSpeed(int number)
    {
        player.ChangeSpeed(number * 0.1f);
        playerSpeed += number * 0.1f;
        textPlayerSpeed.text = playerSpeed.ToString();
    }

    public void ChangePigeonSpeed(int number)
    {
        pigeonSpeed += number * 0.1f;

        for (int i = 0; i < moveStates.Count; i++)
        {
            moveStates[i].ChangeSpeed(number * 0.1f);
        }

        textPigeonSpeed.text = pigeonSpeed.ToString();
    }

    public void ChangeRotate(int number)
    {
        pigeonRotate += number * 0.1f;

        for (int i = 0; i < moveStates.Count; i++)
        {
            moveStates[i].ChangeRotate(number * 0.1f);
        }

        textPigeonRotate.text = pigeonRotate.ToString();
    }*/
}
